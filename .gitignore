# Python
__pycache__/
*.pyc
*.egg-info/
.env/

# Local virtualenv (if created in-tree as `arcticswarm/`)
arcticswarm/bin/
arcticswarm/lib/
arcticswarm/include/
arcticswarm/pyvenv.cfg

# Eval run outputs
results/
arcticswarm/results/

# Content / search caches — never commit cache DBs or run caches
cache/
fetch_cache/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Settings file — never commit real secrets; only the template is tracked
config_files.json
